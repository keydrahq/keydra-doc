# PatternFly ships CSS and font assets that PnP resolution handles poorly, and the
# frontend already made this decision for the same reason. Same nodeLinker here so a
# contributor moving between the two trees meets one layout.
nodeLinker: node-modules
enableGlobalCache: true
