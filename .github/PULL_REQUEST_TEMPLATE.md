<!--
  The manual is generated from the Keydra source tree, so `make docs-check` needs the
  application checked out beside it. It runs the build, the validator and Vale.
-->

## What this changes

<!-- Which pages, and what a reader gets that they did not have before. -->

Fixes #

## Checked

- [ ] `make docs-check` passes — build, validation and Vale
- [ ] Both languages changed, or the change is genuinely one-language
- [ ] Every label quoted in bold is the label the interface shows, in that language
- [ ] Any new screenshot is of the interface in the language of the page it sits on

## If you added a screenshot

<!-- Delete this section if you did not. -->

- [ ] It lives under `content/<lang>/images/` and is named in that language
- [ ] Its alt text is written in that language and says what the reader should look at
- [ ] Nothing in it is real: no real key names, addresses, tokens or anybody's data
